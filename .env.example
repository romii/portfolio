# GitHub username whose repos/activity to show (must match your GitHub profile URL).
GITHUB_USERNAME=

# GitHub personal access token (required for activity chart & latest commit).
# Create one at: https://github.com/settings/tokens
# Scopes needed: none for public repos.
GITHUB_TOKEN=
